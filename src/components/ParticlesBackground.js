import Particles from 'react-particles';
import particlesConfige from './Particles-confige';

export const ParticlesBackground = () => {
  return (
    <Particles params={particlesConfige}>
    
    </Particles>
  )
}
export default ParticlesBackground;